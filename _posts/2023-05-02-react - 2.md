---
categories: [React]
tags: [React, SPA, Component, Hook, JSX, NPM]
img_path: /assets/lib/post-img/
---

```mermaid
flowchart LR
  subgraph Web
		direction TB
    subgraph 초창기 web
			direction RL
			B[HTML]
			subgraph Internet
		  end
end
    subgraph Css
        C[HTML을 심미적으로 보완하기 위해]
      end
    subgraph JS
      D[동적인 상호작용을 위해]
      subgraph Node.js
      E[JS로 컴퓨터 제어하기 위해해]
      end
    end
  end

```
